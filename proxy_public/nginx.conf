
#user http;
worker_processes  1;

#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       mime.types;
    default_type  application/octet-stream;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    #access_log  logs/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    #gzip  on;

    resolver 127.0.0.11  valid=30s;   # Docker's DNS server

    include upstream.conf;

    ssl_certificate      fullchain.pem;
    ssl_certificate_key  privkey.pem;
#        ssl_prefer_server_ciphers  on;
    ssl_session_cache shared:le_nginx_SSL:10m;
    ssl_session_timeout 1440m;
    ssl_session_tickets off;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;
    ssl_ciphers "ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384";
#        grpc_ssl_certificate cert.pem;
#        grpc_ssl_certificate_key cert.key;
#        grpc_ssl_server_name off;

    ####################################################################################################################
    # RPC

    # 1. Osmosis
    server {
        listen 443 ssl http2;
        server_name rpc.osmosis.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_osmosis/$1$is_args$args;
        }
    }

    # 2. Starname
    server {
        listen 443 ssl http2;
        server_name rpc.starname.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_starname/$1$is_args$args;
        }
    }

    # 3. Regen
    server {
        listen 443 ssl http2;
        server_name rpc.regen.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_regen/$1$is_args$args;
        }
    }

    # 4. Akash
    server {
        listen 443 ssl http2;
        server_name rpc.akash.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_akash/$1$is_args$args;
        }
    }

    # 5. Gaia
    server {
        listen 443 ssl http2;
        server_name rpc.cosmoshub.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_cosmoshub/$1$is_args$args;
        }
    }

    # 6. Sentinel
    server {
        listen 443 ssl http2;
        server_name rpc.sentinel.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_sentinel/$1$is_args$args;
        }
    }

    # 7. E-Money
    server {
        listen 443 ssl http2;
        server_name rpc.emoney.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_emoney/$1$is_args$args;
        }
    }

    # 8. Ixo
    server {
        listen 443 ssl http2;
        server_name rpc.ixo.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_ixo/$1$is_args$args;
        }
    }

    # 9. Juno
    server {
        listen 443 ssl http2;
        server_name rpc.juno.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_juno/$1$is_args$args;
        }
    }

    # 10. Sifchain
    server {
        listen 443 ssl http2;
        server_name rpc.sifchain.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_sifchain/$1$is_args$args;
        }
    }

    # 11. Likecoin
    server {
        listen 443 ssl http2;
        server_name rpc.likecoin.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_likecoin/$1$is_args$args;
        }
    }

    # 12. Ki
    server {
        listen 443 ssl http2;
        server_name rpc.kichain.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_kichain/$1$is_args$args;
        }
    }

    # 13. Cyber
    server {
        listen 443 ssl http2;
        server_name rpc.cyber.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_cyber/$1$is_args$args;
        }
    }

    # 14. Cheqd
    server {
        listen 443 ssl http2;
        server_name rpc.cheqd.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_cheqd/$1$is_args$args;
        }
    }

    # 15. Stargaze
    server {
        listen 443 ssl http2;
        server_name rpc.stargaze.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_stargaze/$1$is_args$args;
        }
    }

    # 16. Band
    server {
        listen 443 ssl http2;
        server_name rpc.bandchain.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_bandchain/$1$is_args$args;
        }
    }

    # 17. Chihuahua
    server {
        listen 443 ssl http2;
        server_name rpc.chihuahua.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_chihuahua/$1$is_args$args;
        }
    }

    # 18. Kava
    server {
        listen 443 ssl http2;
        server_name rpc.kava.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_kava/$1$is_args$args;
        }
    }

    # 19. BitCanna
    server {
        listen 443 ssl http2;
        server_name rpc.bitcanna.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_bitcanna/$1$is_args$args;
        }
    }

    # 20. Konstellation
    server {
        listen 443 ssl http2;
        server_name rpc.konstellation.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_konstellation/$1$is_args$args;
        }
    }

    # 21. Omniflix
    server {
        listen 443 ssl http2;
        server_name rpc.omniflixhub.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_omniflixhub/$1$is_args$args;
        }
    }

    # 22. Terra
    server {
        listen 443 ssl http2;
        server_name rpc.terra.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_terra/$1$is_args$args;
        }
    }

    # 23. Vidulum
    server {
        listen 443 ssl http2;
        server_name rpc.vidulum.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_vidulum/$1$is_args$args;
        }
    }

    # 24. Provenance
    server {
        listen 443 ssl http2;
        server_name rpc.provenance.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_provenance/$1$is_args$args;
        }
    }

    # 25. Dig
    server {
        listen 443 ssl http2;
        server_name rpc.dig.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_dig/$1$is_args$args;
        }
    }

    # 26. Gravity-Bridge
    server {
        listen 443 ssl http2;
        server_name rpc.gravitybridge.notional.ventures;
        location ~* ^/(.*) {
            proxy_pass http://rpc_gravitybridge/$1$is_args$args;
        }
    }

    ####################################################################################################################
    # REST/API

    # 1. Osmosis
    server {
        listen 443 ssl http2;
        server_name api.osmosis.notional.ventures;
        location / {
            proxy_pass http://api_osmosis/$1$is_args$args;
        }
    }

    # 2. Starname
    server {
        listen 443 ssl http2;
        server_name api.starname.notional.ventures;
        location / {
            proxy_pass http://api_starname/$1$is_args$args;
        }
    }

    # 3. Regen
    server {
        listen 443 ssl http2;
        server_name api.regen.notional.ventures;
        location / {
            proxy_pass http://api_regen/$1$is_args$args;
        }
    }

    # 4. Akash
    server {
        listen 443 ssl http2;
        server_name api.akash.notional.ventures;
        location / {
            proxy_pass http://api_akash/$1$is_args$args;
        }
    }

    ########################
    # gRPC

    # 1. Osmosis
    server {
        listen 443 ssl http2;
        server_name grpc.osmosis.notional.ventures;
        location / {
            proxy_pass http://grpc_osmosis/$1$is_args$args;
        }
    }

    # 2. Starname
    server {
        listen 443 ssl http2;
        server_name grpc.starname.notional.ventures;
        location / {
            proxy_pass http://grpc_starname/$1$is_args$args;
        }
    }

    # 3. Regen
    server {
        listen 443 ssl http2;
        server_name grpc.regen.notional.ventures;
        location / {
            proxy_pass http://grpc_regen/$1$is_args$args;
        }
    }

    ########################
    # Websocket

    # 1. Osmosis
    server {
        listen 443 ssl http2;
        server_name ws.osmosis.notional.ventures;
        location / {
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $http_host;
            proxy_set_header X-NginX-Proxy true;

            proxy_pass http://rpc_osmosis/websocket;
        }
    }

    # 2. Starname
    server {
        listen 443 ssl http2;
        server_name ws.starname.notional.ventures;
        location / {
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $http_host;
            proxy_set_header X-NginX-Proxy true;

            proxy_pass http://rpc_starname/websocket;
        }
    }

    # 3. Regen
    server {
        listen 443 ssl http2;
        server_name ws.regen.notional.ventures;
        location / {
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $http_host;
            proxy_set_header X-NginX-Proxy true;

            proxy_pass http://rpc_regen/websocket;
        }
    }

}
