
#user http;
worker_processes  1;

#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       mime.types;
    default_type  application/octet-stream;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    #access_log  logs/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    #gzip  on;

    resolver 127.0.0.11  valid=30s;   # Docker's DNS server

    server {
        listen       80;
        server_name  localhost;
        #charset koi8-r;

        #access_log  logs/host.access.log  main;

        location / {
            root   /usr/share/nginx/html;
            index  index.html index.htm;
        }

        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }

        # 1. Osmosis
        location ~* ^/osmosis/(.*) {
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $http_host;
            proxy_set_header X-NginX-Proxy true;

            set $target_osmosis osmosis;
            proxy_pass http://$target_osmosis:26657/$1$is_args$args;
        }

        # 2. Starname
        location ~* ^/starname/(.*) {
            set $target_starname starname;
            proxy_pass http://$target_starname:26657/$1$is_args$args;
        }

        # 3. Regen
        location ~* ^/regen/(.*) {
            set $target_regen regen;
            proxy_pass http://$target_regen:26657/$1$is_args$args;
        }

        # 4. Akash
        location ~* ^/akash/(.*) {
            set $target_akash akash;
            proxy_pass http://$target_akash:26657/$1$is_args$args;
        }

        # 5. Gaia
        location ~* ^/cosmoshub/(.*) {
            set $target_cosmoshub cosmoshub;
            proxy_pass http://$target_cosmoshub:26657/$1$is_args$args;
        }

        # 6. Sentinel
        location ~* ^/sentinel/(.*) {
            set $target_sentinel sentinel;
            proxy_pass http://$target_sentinel:26657/$1$is_args$args;
        }

        # 7. E-Money
        location ~* ^/emoney/(.*) {
            set $target_emoney emoney;
            proxy_pass http://$target_emoney:26657/$1$is_args$args;
        }

        # 8. Ixo
        location ~* ^/ixo/(.*) {
            set $target_ixo ixo;
            proxy_pass http://$target_ixo:26657/$1$is_args$args;
        }

        # 9. Juno
        location ~* ^/juno/(.*) {
            set $target_juno juno;
            proxy_pass http://$target_juno:26657/$1$is_args$args;
        }

        # 10. Sifchain
        location ~* ^/sifchain/(.*) {
            set $target_sifchain sifchain;
            proxy_pass http://$target_sifchain:26657/$1$is_args$args;
        }

        # 11. Likecoin
        location ~* ^/likecoin/(.*) {
            set $target_likecoin likecoin;
            proxy_pass http://$target_likecoin:26657/$1$is_args$args;
        }

        # 12. Ki
        location ~* ^/kichain/(.*) {
            set $target_kichain kichain;
            proxy_pass http://$target_kichain:26657/$1$is_args$args;
        }

        # 13. Cyber
        location ~* ^/cyber/(.*) {
            set $target_cyber cyber;
            proxy_pass http://$target_cyber:26657/$1$is_args$args;
        }

        # 14. Cheqd
        location ~* ^/cheqd/(.*) {
            set $target_cheqd cheqd;
            proxy_pass http://$target_cheqd:26657/$1$is_args$args;
        }

        # 15. Stargaze
        location ~* ^/stargaze/(.*) {
            set $target_stargaze stargaze;
            proxy_pass http://$target_stargaze:26657/$1$is_args$args;
        }

        # 16. Band
        location ~* ^/bandchain/(.*) {
            set $target_bandchain bandchain;
            proxy_pass http://$target_bandchain:26657/$1$is_args$args;
        }

        # 17. Chihuahua
        location ~* ^/chihuahua/(.*) {
            set $target_chihuahua chihuahua;
            proxy_pass http://$target_chihuahua:26657/$1$is_args$args;
        }

        # 18. Kava
        location ~* ^/kava/(.*) {
            set $target_kava kava;
            proxy_pass http://$target_kava:26657/$1$is_args$args;
        }

        # 19. BitCanna
        location ~* ^/bitcanna/(.*) {
            set $target_bitcanna bitcanna;
            proxy_pass http://$target_bitcanna:26657/$1$is_args$args;
        }

        # 20. Konstellation
        location ~* ^/konstellation/(.*) {
            set $target_konstellation konstellation;
            proxy_pass http://$target_konstellation:26657/$1$is_args$args;
        }

        # 21. Omniflix
        location ~* ^/omniflixhub/(.*) {
            set $target_omniflixhub omniflixhub;
            proxy_pass http://$target_omniflixhub:26657/$1$is_args$args;
        }

        # 22. Terra
        location ~* ^/terra/(.*) {
            set $target_terra terra;
            proxy_pass http://$target_terra:26657/$1$is_args$args;
        }
    }

    ###############################
    # GRPC CONFIG
    ###############################

    # 1. Osmosis
    server {
        listen 9001 http2;
        location / {
            set $target_osmosis osmosis;
            grpc_pass $target_osmosis:9090;
        }
    }

   # 2. Starname
    server {
        listen 9002 http2;
        location / {
            set $target_starname starname;
            grpc_pass $target_starname:9090;
        }
    }

    # 3. Regen
    server {
        listen 9003 http2;
        location / {
            set $target_regen regen;
            grpc_pass $target_regen:9090;
        }
    }

    # 4. Akash
    server {
        listen 9004 http2;
        location / {
            set $target_akash akash;
            grpc_pass $target_akash:9090;
        }
    }

    # 5. Gaia
    server {
        listen 9005 http2;
        location / {
            set $target_cosmoshub cosmoshub;
            grpc_pass $target_cosmoshub:9090;
        }
    }

    # 6. Sentinel
    server {
        listen 9006 http2;
        location / {
            set $target_sentinel sentinel;
            grpc_pass $target_sentinel:9090;
        }
    }

    # 7. E-Money
    server {
        listen 9007 http2;
        location / {
            set $target_emoney emoney;
            grpc_pass $target_emoney:9090;
        }
    }

    # 8. Ixo
    server {
        listen 9008 http2;
        location / {
            set $target_ixo ixo;
            grpc_pass $target_ixo:9090;
        }
    }

    # 9. Juno
    server {
        listen 9009 http2;
        location / {
            set $target_juno juno;
            grpc_pass $target_juno:9090;
        }
    }

    # 10. Sifchain
    server {
        listen 9010 http2;
        location / {
            set $target_sifchain sifchain;
            grpc_pass $target_sifchain:9090;
        }
    }

    # 11. Likecoin
    server {
        listen 9011 http2;
        location / {
            set $target_likecoin likecoin;
            grpc_pass $target_likecoin:9090;
        }
    }

    # 12. Ki
    server {
        listen 9012 http2;
        location / {
            set $target_kichain kichain;
            grpc_pass $target_kichain:9090;
        }
    }

    # 13. Cyber
    server {
        listen 9013 http2;
        location / {
            set $target_cyber cyber;
            grpc_pass $target_cyber:9090;
        }
    }

    # 14. Cheqd
    server {
        listen 9014 http2;
        location / {
            set $target_cheqd cheqd;
            grpc_pass $target_cheqd:9090;
        }
    }

    # 15. Stargaze
    server {
        listen 9015 http2;
        location / {
            set $target_stargaze stargaze;
            grpc_pass $target_stargaze:9090;
        }
    }

    # 16. Band
    server {
        listen 9016 http2;
        location / {
            set $target_bandchain bandchain;
            grpc_pass $target_bandchain:9090;
        }
    }

    # 17. Chihuahua
    server {
        listen 9017 http2;
        location / {
            set $target_chihuahua chihuahua;
            grpc_pass $target_chihuahua:9090;
        }
    }

    # 18. Kava
    server {
        listen 9018 http2;
        location / {
            set $target_kava kava;
            grpc_pass $target_kava:9090;
        }
    }

    # 19. BitCanna
    server {
        listen 9019 http2;
        location / {
            set $target_bitcanna bitcanna;
            grpc_pass $target_bitcanna:9090;
        }
    }

    # 20. Konstellation
    server {
        listen 9020 http2;
        location / {
            set $target_konstellation konstellation;
            grpc_pass $target_konstellation:9090;
        }
    }

    # 21. Omniflix
    server {
        listen 9021 http2;
        location / {
            set $target_omniflixhub omniflixhub;
            grpc_pass $target_omniflixhub:9090;
        }
    }

    # 22. Terra
    server {
        listen 9022 http2;
        location / {
            set $target_terra terra;
            grpc_pass $target_terra:9090;
        }
    }
}
